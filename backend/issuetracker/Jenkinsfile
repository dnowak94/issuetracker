#!/usr/bin/env groovy
pipeline {
    agent {
        dockerfile { dir 'backend/issuetracker' }
    }
    stages {
        stage('build') {
            steps {
                sh 'ls target/'
            }
        }
    }
}